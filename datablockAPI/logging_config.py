"""datablockAPI - Logging ConfigurationStructured logging setup for the package."""import loggingimport sysfrom .config import configdef setup_logging():    """Setup logging configuration."""    logging.basicConfig(        level=getattr(logging, config.logging.level.upper()),        format=config.logging.format,        stream=sys.stdout    )    # Create logger for the package    logger = logging.getLogger('datablockAPI')    logger.setLevel(getattr(logging, config.logging.level.upper()))    return logger# Global logger instancelogger = setup_logging()